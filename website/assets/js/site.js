window.addEventListener('load',()=>document.getElementById('loader')?.classList.add('hidden'));
const nav=document.querySelector('.fp-navbar'),backToTop=document.getElementById('backToTop');
function handleScroll(){nav?.classList.toggle('scrolled',window.scrollY>40);backToTop?.classList.toggle('show',window.scrollY>500)}
window.addEventListener('scroll',handleScroll);handleScroll();backToTop?.addEventListener('click',()=>window.scrollTo({top:0,behavior:'smooth'}));
const themeToggle=document.getElementById('themeToggle');if(localStorage.getItem('fitpro-theme')==='dark')document.body.classList.add('dark-mode');themeToggle?.addEventListener('click',()=>{document.body.classList.toggle('dark-mode');localStorage.setItem('fitpro-theme',document.body.classList.contains('dark-mode')?'dark':'light')});
const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting)entry.target.classList.add('visible')}),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>observer.observe(el));
document.querySelectorAll('[data-count]').forEach(counter=>{const target=parseInt(counter.dataset.count||'0',10);let started=false;const countObserver=new IntersectionObserver(entries=>{if(entries[0].isIntersecting&&!started){started=true;let current=0,step=Math.max(1,Math.ceil(target/70));const timer=setInterval(()=>{current+=step;if(current>=target){current=target;clearInterval(timer)}counter.textContent=current.toLocaleString()},18)}});countObserver.observe(counter)});
document.querySelectorAll('[data-filter-target]').forEach(button=>button.addEventListener('click',()=>{const target=button.dataset.filterTarget,group=button.dataset.filterGroup;document.querySelectorAll(`[data-filter-group="${group}"]`).forEach(btn=>btn.classList.remove('active'));button.classList.add('active');document.querySelectorAll(`[data-filter-item="${group}"]`).forEach(item=>{item.style.display=target==='all'||item.dataset.category===target?'':'none'})}));
document.getElementById('bmiForm')?.addEventListener('submit',event=>{event.preventDefault();const weight=parseFloat(document.getElementById('bmiWeight').value),heightCm=parseFloat(document.getElementById('bmiHeight').value),result=document.getElementById('bmiResult');if(!weight||!heightCm||!result)return;const height=heightCm/100,bmi=weight/(height*height);let label='Healthy range';if(bmi<18.5)label='Underweight';else if(bmi>=25&&bmi<30)label='Overweight';else if(bmi>=30)label='Obese range';result.innerHTML=`<strong>${bmi.toFixed(1)}</strong><span>${label}</span>`});
document.querySelectorAll('[data-gallery-src]').forEach(item=>item.addEventListener('click',()=>{const modalImg=document.getElementById('lightboxImage');if(modalImg)modalImg.src=item.dataset.gallerySrc}));
