window.addEventListener('load',()=>document.getElementById('loadingScreen')?.classList.add('hidden'));
const nav=document.querySelector('.website-navbar'),topBtn=document.getElementById('backToTop');
function scrollUi(){nav?.classList.toggle('scrolled',window.scrollY>40);topBtn?.classList.toggle('show',window.scrollY>500)}
window.addEventListener('scroll',scrollUi);scrollUi();topBtn?.addEventListener('click',()=>window.scrollTo({top:0,behavior:'smooth'}));
if(localStorage.getItem('fitpro-theme')==='dark')document.body.classList.add('dark-mode');
document.getElementById('themeToggle')?.addEventListener('click',()=>{document.body.classList.toggle('dark-mode');localStorage.setItem('fitpro-theme',document.body.classList.contains('dark-mode')?'dark':'light')});
const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting)entry.target.classList.add('aos-animate')}),{threshold:.12});document.querySelectorAll('[data-aos]').forEach(el=>observer.observe(el));
document.querySelectorAll('[data-count]').forEach(counter=>{const target=parseInt(counter.dataset.count||'0',10);let started=false;const obs=new IntersectionObserver(entries=>{if(entries[0].isIntersecting&&!started){started=true;let current=0,step=Math.max(1,Math.ceil(target/70));const timer=setInterval(()=>{current+=step;if(current>=target){current=target;clearInterval(timer)}counter.textContent=current.toLocaleString()},18)}});obs.observe(counter)});
document.querySelectorAll('.filter-btn,[data-filter]').forEach(btn=>btn.addEventListener('click',()=>{const filter=btn.dataset.filter||btn.dataset.filterTarget||'all';document.querySelectorAll('.filter-btn,[data-filter]').forEach(b=>b.classList.remove('active'));btn.classList.add('active');document.querySelectorAll('.class-item,.gallery-item,[data-category]').forEach(item=>{const category=item.dataset.category||'';item.style.display=filter==='all'||category===filter?'':'none'})}));
document.getElementById('bmiForm')?.addEventListener('submit',e=>{e.preventDefault();const w=parseFloat(document.getElementById('bmiWeight')?.value),h=parseFloat(document.getElementById('bmiHeight')?.value),out=document.getElementById('bmiResult');if(!w||!h||!out)return;const bmi=w/((h/100)*(h/100));let label='Healthy range';if(bmi<18.5)label='Underweight';else if(bmi>=25&&bmi<30)label='Overweight';else if(bmi>=30)label='Obese range';out.innerHTML=`<strong>${bmi.toFixed(1)}</strong><span class="d-block">${label}</span>`});
function setPlan(name,price){document.getElementById('selectedPlan')?.setAttribute('value',name);document.getElementById('planDisplay')&&(document.getElementById('planDisplay').textContent=`${name} - KES ${Number(price).toLocaleString()}`)}
function setTrainerDetails(name,specialization){document.getElementById('trainerModalTitle')&&(document.getElementById('trainerModalTitle').textContent=name);document.getElementById('trainerModalSpecialization')&&(document.getElementById('trainerModalSpecialization').textContent=specialization)}
function setClassDetails(name){document.getElementById('classModalTitle')&&(document.getElementById('classModalTitle').textContent=name)}
